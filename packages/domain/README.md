# @top-seed/domain

Pure business rules for Top Seed (queueing, ratings, payments, state transitions).

Phase 0: placeholder only. Implement domain logic in **Phase 1** per `docs/specs/backend/queueing-and-ratings.md`.

Rules:

- No Fastify, Prisma, or React imports in this package.
- Imported by `apps/api` application use cases only.
