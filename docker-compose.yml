services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: topseed
    volumes:
      - topseed_pg_data:/var/lib/postgresql/data

volumes:
  topseed_pg_data:
